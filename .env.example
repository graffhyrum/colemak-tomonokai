APP_HOST=localhost
APP_PORT=42069

# Test configuration
# Set to true to run tests against all browser/shape/layout combinations (90 projects)
# Set to false or unset to run against subset (1 browser × 1 shape × 2 layouts = 2 projects)
PLAYWRIGHT_FULL_TEST=false

