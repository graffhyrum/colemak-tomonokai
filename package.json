{
	"name": "colemak-tomonokai",
	"type": "module",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"typecheck": "tsgo --noEmit --pretty",
		"dev": "bun scripts/dev.ts",
		"build": "bun build index.html --outdir dist --target browser",
		"test:e2e": "bun scripts/pwTest.ts",
		"lint": "bunx biome lint",
		"format": "bunx biome format",
		"check": "bunx biome check",
		"check:fix": "bunx biome check --fix",
		"vet": "bun check:fix && bun typecheck && bun test:e2e"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8",
		"@changesets/cli": "^2.29.8",
		"@playwright/test": "^1.57.0",
		"@types/bun": "^1.3.3"
	},
	"peerDependencies": {
		"@typescript/native-preview": "^7.0.0-dev.20251202.1"
	},
	"dependencies": {
		"arkenv": "^0.7.6",
		"arktype": "^2.1.28"
	}
}
