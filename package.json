{
	"name": "colemak-tomonokai",
	"type": "module",
	"private": true,
	"scripts": {
		"typecheck": "tsgo --noEmit --pretty",
		"dev": "bun index.html",
		"test": "playwright test",
		"lint": "biome lint",
		"format": "bunx biome format",
		"check": "bunx biome check",
		"check:fix": "bunx biome check --fix",
		"vet": "bun check:fix && bun typecheck && bun run test"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8",
		"@playwright/test": "^1.57.0",
		"@types/bun": "^1.3.5"
	},
	"peerDependencies": {
		"@typescript/native-preview": "^7.0.0-dev.20251202.1"
	},
	"dependencies": {
		"@total-typescript/ts-reset": "^0.6.1"
	}
}
