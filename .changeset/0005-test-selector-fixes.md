---
"colemak-tomonokai": patch
---

Fix test selectors and navigation timeouts to resolve test failures caused by component refactoring. Update keyboard selectors from .keyboard to .cheatsheet, active key selectors from .key.active to .key.currentLevelKeys, and add proper timeout handling for navigation button clicks.